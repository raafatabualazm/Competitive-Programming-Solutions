#include <iostream>

using namespace std;

int main()
{
    string out;
    int n,k,a,b;
    cin >> n >> k >> a >> b;
    for ( int i =0; i <n; i++) {
        if ( a > b) {
            if ( a > k) {

            }
        }
    }
    return 0;
}
